# Built-in skill profiles. See docs/vendor-research.md.
artifacts = ["skills"]

[harness.claude]
profile = "claude"
format = "xml"
structure = "nested"
artifacts = ["skills"]

[harness.claude.variables]
model_strong = "opus"
model_normal = "sonnet"
model_weak = "haiku"

[harness.claude.tools]
bash = "Bash"
read = "Read"
write = "Write"
edit = "Edit"
glob = "Glob"
grep = "Grep"
task = "Task"

[harness.claude.references.skill]
output = "/{{.Name}}"

[harness.claude-upload]
profile = "claude-upload"
format = "xml"
structure = "nested"
artifacts = ["skills"]

[harness.codex]
profile = "codex"
format = "directives"
structure = "nested"
artifacts = ["skills"]

[harness.codex.references.skill]
output = "${{.Name}}"

[harness.chatgpt]
profile = "chatgpt"
format = "directives"
structure = "nested"
artifacts = ["skills"]

[harness.chatgpt.references.skill]
output = "@{{.Name}}"

[harness.opencode]
profile = "opencode"
format = "directives"
structure = "nested"
artifacts = ["skills"]

[harness.opencode.references.skill]
output = 'skill({name: "{{.Name}}"})'

[harness.gemini]
profile = "gemini"
format = "directives"
structure = "nested"
artifacts = ["skills"]

[harness.gemini.references.skill]
output = 'activate_skill({name: "{{.Name}}"})'

[harness.github]
profile = "github"
format = "directives"
structure = "nested"
artifacts = ["skills"]

[harness.cursor]
profile = "cursor"
format = "directives"
structure = "nested"
artifacts = ["skills"]

[harness.cursor.references.skill]
output = "/{{.Name}}"

[harness.agentskills]
profile = "agentskills"
format = "directives"
structure = "nested"
artifacts = ["skills"]
